{
  "name": "eternal-architect",
  "version": "1.0.0",
  "description": "Eternal Architect",
  "scripts": {
    "dev": "concurrently \"cd backend && source venv2/bin/activate && uvicorn main:app --reload --port 3002\" \"npm run dev --prefix client\"",
    "install:all": "npm install && cd backend && python3 -m venv venv2 && source venv2/bin/activate && pip install -r requirements.txt && cd .. && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
